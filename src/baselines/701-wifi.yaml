esp32:
  board: wt32-eth01

packages:
  wifi: !include  
    file: /workspace/include/wifi.yaml

# "All Device" baseline stuff here

<<: !include 
  file: /workspace/include/esp32-device-baseline.yaml
  vars:
    device_hostname: "701-esphome"
    device_comment: "Basic config for board 701 connected on WiFi."



